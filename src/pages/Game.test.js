import { screen , render} from '@testing-library/react';
import Game from './Game'

describe('Game Page', () => {
    it('should display the list of words', () => {
        render(<Game />)
        expect()
    });
});